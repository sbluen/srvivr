Welcome to the admin panel.

    <% if flash[:notice] %>
			<p id="notice"><%= flash[:notice] %></p>
		<% end %>

<!-- source: http://railscasts.com/episodes/37-simple-search-form -->
<%= form_tag 'admin', :method => 'get' do %>
  <p>
  	<%= label_tag(:search, "Search for user:") %>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
  </p>
<% end %>

NOTE: This page is under construction.
  <div id="search">
  	<!--This logic once was needed to avoid an unbound variable exception-->
  	<% if not @users.nil? then %>
    <%= render :partial => 'results' %>
    <%end%>
  </div>

